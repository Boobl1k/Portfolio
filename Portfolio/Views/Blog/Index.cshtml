@using Portfolio.Extensions
@model IQueryable<Portfolio.Entity.Post>

<!-- +++++ Posts Lists +++++ -->
<!-- +++++ First Post +++++ -->
@{ var white = false; }
@foreach (var post in Model)
{
    @Html.ShowBlog(post, white = !white)
}